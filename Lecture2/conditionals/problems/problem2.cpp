#include <iostream>
using namespace std;

int main() {
  int a;
  cin >> a;

  if (a >= b && a >= c) {
    cout << a << endl;
  } else if (b >= c && b >= a) {
    cout << b << endl;
  } else cout << c << endl;

  cout << abs(a);
  cout << endl;
}