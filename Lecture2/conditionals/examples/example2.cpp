#include <iostream>
using namespace std;

int main() {
  int a; cin >> a;
  // if (a == 10) cout << to_string(a) + "smth";
  // if (a == 11) cout << to_string(a) + "smth";
  // if (a == 12) cout << to_string(a) + "smth";
  // if (a == 13) cout << to_string(a) + "smth";
  // if (a == 14) cout << to_string(a) + "smth";
  // if (a == 15) cout << to_string(a) + "smth";
  // if (a == 16) cout << to_string(a) + "smth";

  switch(a) { 
    case 1: {
      cout << "a is 1";
      break;
    }
    case 2: {
      cout << "a is 1";
      break;
    }
    case 3: {
      cout << "a is 1";
      break;
    }
    case 4: {
      cout << "a is 1";
      break;
    }
  }
}